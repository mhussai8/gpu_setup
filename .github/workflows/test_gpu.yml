name: train-my-model
on: [push]
jobs:
  train:
    runs-on: [self-hosted]
    steps:
      - uses: actions/checkout@v3
      - name: cml_run
        run: |
          CODEBLOCK="\'\'\'"
          echo $CODEBLOCK > report.md
          nvidia-smi >> report.md
          echo $CODEBLOCK > report.md
