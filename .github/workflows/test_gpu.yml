name: train-my-model
on: [push]
jobs:
  train:
    runs-on: [self-hosted]
    steps:
      - uses: actions/checkout@v3
      - name: cml_run
        run: |
          echo 'Hi from CML!' >> report.md
          cml send-comment report.md
